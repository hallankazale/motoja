# Server-only secrets. Configure in Supabase > Edge Functions > Secrets.
# Use contracted/self-hosted services compatible with Nominatim and OSRM.
# Validate motorcycle routing and local roads before setting routing_ready.
GEOCODER_BASE_URL=
ROUTER_BASE_URL=
MAPS_PROVIDER_TOKEN=
